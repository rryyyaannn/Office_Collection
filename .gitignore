# Dependências, build e Vercel (em qualquer app)
node_modules/
dist/
dist-ssr/
.vercel/
*.local

# 🔒 Documentos internos e de clientes — NÃO versionar (troca interna do time)
/docs/

# Pasta stray de skills na RAIZ (não confundir com .claude/skills, que É versionado)
/skills/

# Config local do Claude (segredos/permissões da máquina) — o resto de .claude/ é versionado
.claude/settings.local.json

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db

# Ambiente
.env
.env.*
!.env.example

# 🔒 Supabase — NÃO versionar (decisão do Ryan): migrations/seed/functions + segredos ficam locais
/supabase/

# Workspace do Claude — local apenas (decisão: não versionar)
.claude/
