# Fonte de dados: "mock" (padrão, roda sem backend) ou "supabase" (quando houver DEV)
VITE_DATA_SOURCE=mock

# Supabase DEV (preencher quando o ambiente de dev for liberado — NUNCA produção)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
